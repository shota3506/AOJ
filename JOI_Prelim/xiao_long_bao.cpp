//Xiao Long Bao
#include <iostream>
#include <vector>
#include <climits>

using namespace std;

int main() {
    int N;
    cin >> N;
    int D[N], A[N];
    vector<int> V[N];
    for(int i = 0; i < N; i++) {
        cin >> D[i];
    }
}